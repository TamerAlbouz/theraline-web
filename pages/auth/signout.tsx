const SignOutPage = () => {
  const onSignOut = () => {};

  return (
    <button
      className="text-3xl font-semibold text-textColor"
      onClick={onSignOut}
    >
      Sign out
    </button>
  );
};

export default SignOutPage;
